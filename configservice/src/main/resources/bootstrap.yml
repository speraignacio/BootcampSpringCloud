server:
  port: 8081

spring:
  cloud:
    server:
      git:
        # url de respositorio donde estan ubicados los yml de los proyectos
        uri: https://github.com/speraignacio/BootcampSpringCloud.git
        searchPaths: config-data
  security:
    user:
      name: root
      password: s3cr3t  
